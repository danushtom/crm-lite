[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dracara-scoring"
version = "1.0.0"
description = "Weighted opportunity score, shared by the API and the Celery worker."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
