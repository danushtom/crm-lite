fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.10.5
pydantic-settings>=2.7.0
email-validator>=2.2.0
PyJWT[crypto]>=2.10.1
httpx>=0.28.1
python-multipart>=0.0.20
cryptography>=44.0.0
slowapi>=0.1.9
# IANA timezone database. Windows ships none, and slim container images often omit it,
# so zoneinfo cannot resolve 'Asia/Kolkata' without this.
tzdata>=2024.2

# Shared scoring model, installed from the workspace so the API and the worker
# run the identical implementation rather than two copies that can drift.
-e ../../packages/scoring

# Testing
pytest>=8.3.4
pytest-asyncio>=0.25.0
